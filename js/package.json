{
  "name": "jupyterlab-singleton-provider",
  "version": "1.0.0",
  "description": "Example of a JupyterLab extension that provides e.g. a webcomponent singleton to other extensions",
  "repository": "git@github.com:timkpaine/jupyterlab-singleton-provider.git",
  "author": "the jupyterlab-singleton-provider authors <t.paine154@gmail.com>",
  "license": "Apache-2.0",
  "keywords": [
    "jupyter",
    "jupyterlab",
    "jupyterlab-extension"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/**/*.{d.ts,eot,gif,html,jpg,js,js.map,json,png,svg,woff2,ttf}",
    "style/**/*.css"
  ],
  "jupyterlab": {
    "extension": "lib/index.js",
    "outputDir": "../jupyterlab_singleton_provider/labextension",
    "discovery": {
      "server": {
        "base": {
          "name": "jupyterlab_singleton_provider"
        },
        "managers": [
          "pip"
        ]
      }
    },
    "sharedPackages": {
      "jupyterlab-singleton-provider": {
        "bundled": true,
        "singleton": true
      }
    }
  },
  "scripts": {
    "build:babel": "babel src/ --source-maps --out-dir lib/",
    "build:extension": "mkdirp ../jupyterlab_singleton_provider/extension/ && cpy --flat 'src/extension/*' '../jupyterlab_singleton_provider/extension/'",
    "build:nbextension": "mkdirp ../jupyterlab_singleton_provider/nbextension/static/ && cpy --flat 'src/notebook.js' '../jupyterlab_singleton_provider/nbextension/static/'",
    "build:labextension": "rimraf ../jupyterlab_singleton_provider/labextension && jupyter-builder build .",
    "build": "pnpm clean && pnpm build:babel && pnpm build:extension && pnpm build:labextension && pnpm build:nbextension",
    "clean": "rimraf lib",
    "fix": "oxlint --fix src/ tests/ && oxfmt --write \"src/**/*.js\" \"tests/**/*.js\"",
    "lint": "oxlint src/ tests/ && oxfmt --check \"src/**/*.js\" \"tests/**/*.js\"",
    "preinstall": "npx only-allow pnpm",
    "prepublishOnly": "pnpm run build",
    "test": "jest --coverage --collectCoverageFrom=src/*.{js}"
  },
  "dependencies": {
    "@jupyterlab/application": "^4.6.3",
    "@jupyterlab/apputils": "^4.7.3",
    "@jupyterlab/notebook": "^4.6.3",
    "@jupyterlab/services": "^7.6.3",
    "@lumino/coreutils": "^2.2.3",
    "@lumino/disposable": "^2.1.6"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@babel/core": "^8.0.1",
    "@babel/preset-env": "^8.0.2",
    "@jupyter/builder": "^1.2.3",
    "@jupyterlab/core-meta": "^4.6.3",
    "babel-jest": "^30.5.1",
    "cpy-cli": "^7.0.0",
    "css-loader": "^7.1.4",
    "isomorphic-fetch": "^3.0.0",
    "jest": "^30.5.1",
    "jest-environment-jsdom": "^30.5.1",
    "jest-junit": "^17.0.0",
    "jest-transform-css": "^6.0.3",
    "mkdirp": "^3.0.1",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "rimraf": "^6.1.3",
    "style-loader": "^4.0.0"
  }
}
